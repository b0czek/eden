---
"@edenapp/sdk": patch
---

Isolate each Eden host in an independently disposable runtime and add Node and
real-Electron integration coverage for lifecycle, permissions, persistence,
views, IPC, processes, and shutdown behavior.
