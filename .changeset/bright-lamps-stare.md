---
"@edenapp/scripts": patch
---

Fix the standalone `eden-build dev` seed generation to include default system autostart settings and to create the default demo user as a `vendor`, so dev hosts start with shell utilities and unrestricted access by default.
