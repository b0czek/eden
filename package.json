{
  "name": "eden",
  "version": "0.1.0",
  "description": "A modular Electron desktop environment with extensible app support",
  "main": "dist/main/index.js",
  "author": "Eden Team",
  "license": "MIT",
  "scripts": {
    "build": "npm run clean && tsc && tsc -p tsconfig.renderer.json && npm run copy-assets",
    "copy-assets": "cp src/renderer/index.html dist/renderer/ && cp src/renderer/app-preload.js dist/renderer/",
    "dev": "npm run build && electron .",
    "start": "electron .",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.7",
    "@types/node": "^20.10.0",
    "electron": "^32.0.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "adm-zip": "^0.5.16"
  }
}
